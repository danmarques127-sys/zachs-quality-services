# Deployment

## Recommended
- Deploy as a normal website at the domain root (e.g., https://example.com/).

## Notes
- Absolute paths assume the site is served from `/`.
- If hosting under a subpath (e.g., GitHub Pages project site), you may need a base-path strategy.

